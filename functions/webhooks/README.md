# Webhooks placeholder

Use this folder for any incoming webhooks you may need later (for example, to sync judging events).
Add a new subfolder per webhook function, mirroring the pattern from `request-otp` and `verify-otp`.
