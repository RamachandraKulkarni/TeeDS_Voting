# Assets

Drop contest logos, collaborator marks, and partner badges in this folder. You can organize them inside the `logos/` subfolder or add discipline-specific directories as the collection grows.

When you provide the final files, import them from `src/assets/...` anywhere in the React app.
